# にじいろ ぽちゃん！しぶきラボ

4歳児向け・水飛沫が主役の3Dおもちゃ実験ラボです。Three.js で作った、
スマホ・タブレットのブラウザだけで遊べる「なげて・おとして・しぶきを見る」
おもちゃ箱です。ボタンや文字を読む必要はほとんどなく、指でつまんで、
はなすだけで遊べます。

## これは何？

飛び込み台の上でおもちゃをつかみ、プールに落とす（または投げる）と、
盛大な水しぶきが上がる——それだけのシンプルな実験ラボです。主役は
「しぶき」そのもの。おもちゃの種類・落とす高さ・投げ方によって、しぶきの
形がまったく変わるので、何度でも「もう1回！」と試したくなるように作って
あります。

## あそびかた

白いウサギが飛び込み台の上にいて、おもちゃを箱から取り出して持って
くれています。子どもがすることはシンプルです。

1. 画面下のボタンでおもちゃをえらぶ（🏓🎳🏖🥏🥤🧽🛟🍮）
   → ウサギがいまの台の上でおもちゃを箱にしまい、新しいおもちゃを
   取り出して両手で持ちあげてくれます。
2. **飛び込み台そのものをタップ**すると、ウサギがはしごを登って
   （またはおりて）低・中・高の台を移動します（画面左のボタンは
   廃止しました。台を直接タップしてください）。
3. 画面（水中・空）を指でドラッグすると、狙いを定める点線の弧が
   表示され、ウサギがその方向へ体を傾けます。指をはなすとウサギが
   ぶんと振りかぶって、狙った方向へおもちゃを投げてくれます
   （ただタップするだけなら、まっすぐ下にぽとんと落とします）。
4. おもちゃが空中にあるあいだ・水しぶきが上がっているあいだは、
   カメラがおもちゃを追いかけ、着水後もしぶきにぴったり寄り添って
   ロックされます（以前のように、しぶきを見る前にすぐ飛び込み台へ
   戻ってしまうことはありません）。このあいだは画面下のボタンも
   自動的に隠れるので、着地地点にボタンが重なることもありません。
   しぶきが落ち着くと、カメラはゆっくり台へパンして戻り、ボタンも
   フェードインし、ウサギが次のおもちゃを取り出して待っていてくれます。

**台をタップして選ぶ・ドラッグして狙う・はなして投げる。** 台の高さと
おもちゃの組み合わせで、飛沫の大きさ・形・水中の泡のようすがぜんぶ
変わります。

## 8つのおもちゃ

| おもちゃ | 絵文字 | 特徴 |
|---|---|---|
| ピンポンだま | 🏓 | 軽くてぷかぷか、しぶきは小さめ |
| おもいボール | 🎳 | ずっしり重く、深く沈んで力強いしぶき |
| ビーチボール | 🏖 | 大きくて軽い、ぼふんと広がるしぶき |
| えんばん | 🥏 | 平たく落ちると水面いっぱいに広がる、水面をなでるように入ると小さく飛沫が立つ |
| コップ | 🥤 | 上向き・下向きで空気の抱え方が変わる（下向きで落ちると泡がぶくぶく） |
| スポンジ | 🧽 | やわらかくてびちゃびちゃ、しぶきもふんわり |
| うきわ | 🛟 | 輪っかの形なので空気を少し抱え込み、ぷかぷか浮く |
| ゼリーボール | 🍮 | ぷるぷる揺れながら着水、はずむしぶき |

## 3つの飛び込み台

| 台 | 高さ |
|---|---|
| 低い台 | 2.2m |
| 中くらいの台 | 4.4m |
| 高い台 | 7.2m |

台が高いほど、おちる勢いが強くなり、しぶきも大きく・力強くなります。

## そらのだい（メガ着水）

タワーの上のほう、気球のゴンドラに気づいたら――そこが4つめの発射台
「そらのだい」です。

1. 気球（ストライプ模様の熱気球＋ゴンドラかご）を**タップ**すると、
   ウサギがタワーのてっぺんまで登ってゴンドラに乗り込み、気球がぐんぐん
   上昇していきます（雲の層を2枚くぐり抜けます）。
2. てっぺんに着くと、ウサギがゴンドラの中の「おおきいクレート」から
   両手で持ち上げるように**ジャイアントおもちゃ**を取り出します
   （ずっしり重そうに、よろけながら）。
3. 画面をドラッグしてねらいを定め、はなすと、ウサギが渾身の力で
   ゆっくり大きく振りかぶって投げます（`heaveThrow`）。
4. ジャイアントおもちゃは雲を抜けてはるか下のプールへ長い距離を落ちて
   いきます。着水の直前は一瞬静かになり（無音の「ため」）、そのあと――

   **7段階の「メガ着水」**が起こります: ①フラッシュ＋衝撃の水面ディスク
   → ②真っ白に沸き立つドーム → ③輪状の壁のような大波＋プールサイドへ
   あふれるオーバーフロー → ④ふつうのジェットの何倍も太い・厚みのある
   ガラス質の水柱（高さ6〜8m）→ ⑤きのこ雲のような水しぶきの雨 →
   ⑥プール全体がしばらく揺れる「スロッシング」→ ⑦白い泡がじわじわと
   引いていく。着水の瞬間には、ゴンドラからウサギの反応を見下ろす
   カメラカットも一瞬入ります。
5. 着水後も気球はそらに浮かんだまま。ウサギは次のジャイアントおもちゃを
   取り出して、また「そらのだい」から投げつづけられます。中・低・高の
   台をタップすると、気球がゆっくり降りてきてウサギが降り、いつもの
   飛び込み台での遊びに戻ります。

### 3つのジャイアントおもちゃ

| おもちゃ | 絵文字 | 特徴 |
|---|---|---|
| ジャイアントおもりボール | 🎳 | ふつうのおもいボールの巨大版。まっすぐ深く沈み、力強いメガ着水 |
| ジャイアントゼリーボール | 🍮 | 着水の衝撃で**6つのかけらに分裂**、それぞれが別々に水しぶきをあげる「ゼリーの雨」に |
| ジャイアントビーチボール | 🏖 | 深く沈み込んだあと勢いよく浮かび上がり（サージ）、もう一段ぴょんと跳ねて2つめの波を作る |

## しぶきのルール（なぜそうなる？）

水しぶきは、いつも同じ見た目ではなく、次のような「わけ」に沿って変化します。

- **高いところから／重いおもちゃほど** → しぶきが大きく、力強くなる
- **平たい面から着水すると**（えんばんを寝かせて落とす、など）→ しぶきは
  横に広く低く広がり、水中のくぼみは浅い
- **とがった・まるい形できれいに刺さると** → しぶきは細く高いクラウン状に
  立ちのぼり、水中に深いくぼみができて、あとから勢いよく水柱が上がる
- **斜めに投げると** → しぶきは投げた方向に片寄って扇状に広がる
- **コップを口が下向きで落とすと** → 中に空気を抱え込んでいるので、
  着水したときに大きな「ぶくぶくっ」という泡がはじける（口が上向きだと
  ほとんど泡は出ない）
- **やわらかいおもちゃ（スポンジ・ゼリー）ほど** → しぶきはぷるぷる・
  びちゃびちゃした、まるみのある見た目になる

## ローカルでの起動方法

ビルド不要・依存パッケージ不要のプレーンな ES モジュール構成です。任意の
静的ファイルサーバーで配信するだけで動きます。

```bash
cd (このリポジトリのルート)
python3 -m http.server 8000
# ブラウザで http://localhost:8000 を開く
```

Python がなければ、`npx serve` や `php -S localhost:8000` など、静的
ファイルを返せるサーバーなら何でも構いません。

**対応環境**: iPhone / iPad の Safari を想定して作られています。縦向き
（ポートレート）・横向き（ランドスケープ）どちらでも遊べるようレイアウトが
調整されます。

## 技術概要

- **Three.js r170** を `vendor/three.module.js` としてリポジトリに同梱
  （`index.html` の importmap で `"three"` を解決）。ビルドステップなし、
  TypeScriptなし、外部npm依存なし。プレーンな ES モジュールのみ。
- **描画（しぶき: フォトリアル版）**: しぶき関連の描画は「グラブパス
  リフラクション」ハイブリッド構成に刷新（`src/watershading.js` +
  `src/main.js` の2パス描画 / `src/splash.js` / `src/droplets.js` /
  `src/underwater.js`）。
  - **グラブパスリフラクション**: 1フレームにつき最大2回描画（不透明な
    シーン→しぶき関連が何もなければそこで終了）。しぶきが発生している
    間だけ、①レイヤー0（通常シーン）を描画→②その描画結果をテクスチャに
    キャプチャ（`GrabPass`）→③しぶき専用レイヤー（`WATER_LAYER`）だけを
    クリアせずに重ね描画、という2パス構成。しぶき系マテリアルは共有
    GLSL チャンク（`WATER_GLSL` / `waterGlslCommon`）の `waterShade()` で
    フレネル反射・空サンプル・法線オフセットによる背景リフラクション・
    Beer-Lambert吸収による水の色づけ・太陽グリントを一括計算する。
  - **クラウン（王冠しぶき）**: 96×24分割の解析的な回転体メッシュ。
    立ち上がり→減速→リム肥厚→指状分裂（フィンガー）→落下、を頂点
    シェーダーで時間発展させ、フィンガー先端のちぎれで水滴を発生。
    厚み（薄いほど透明）を頂点属性として持たせ、フラグメントで
    フローノイズによる法線ゆらぎを加えてリフラクションが揺らめく。
  - **水滴インポスター**: `InstancedMesh` 1つでカメラ正面向きの
    球インポスターを描画（フラグメントでUVから球法線を再構成、円外は
    discard）。速度方向にプロレート形状で伸長、太陽グリントの疑似
    ブルームを追加。別の `InstancedMesh` でソフトな飛沫（ミスト）を
    非リフラクション・通常αブレンドで描画し、水滴の「白さ」を担当。
  - **水中ジェット（Worthington jet）**: なめらかなガラス質のチューブ
    メッシュ。上昇→自然なオーバーシュート→首のくびれ（ネッキング）→
    ピンチオフ（先端が1〜3個の衛星水滴として分離）→残った柱が沈んで
    消える、を時間発展させ、局所半径から厚みを算出してリフラクション/
    吸収に反映。
  - 描画コール数の目安: アイドル時 約22コール、3つのしぶきが重なる
    バーストでも 約40コール前後（予算 <80 コール以内）。しぶきが何も
    無いときは2パス目の描画自体をスキップ（`__lab.waterDebug()`で確認可）。
  - 上記に加え、水面シェーダー（フレネル風の輝き、最大12本の同時リング
    波紋）は変更なし。1粒ずつ Mesh を作るような重い実装は避けてある。
- **物理**: 自前実装の簡易物理エンジン（`src/physics.js`）。放物運動、
  水面での減速・浮力、跳ね返り、着水判定から `ImpactSpec`（着水点・速度・
  エネルギー・扁平度・斜め度・空気の抱え込み量など）を算出し、しぶき・
  水中エフェクト・カメラ・音のすべてがこの1つの値を参照して連動する
  設計（`docs/CONTRACTS.md` 参照）。
- **音**: サンプル音源を使わない、Web Audio API によるプロシージャル生成
  （`src/audio.js`）。
- **モジュール構成**: 環境・水面・上面しぶき・水中しぶき・おもちゃ・物理・
  入力・カメラ&音、それぞれ担当ファイルに分離（詳細は
  `docs/CONTRACTS.md`）。

### デバッグ用 API — `window.__lab`

自動テスト・QA用に、ページから直接おもちゃを操作できるデバッグ API を
`window.__lab` として公開しています（アプリの通常プレイには不要）。

```js
window.__lab.ready        // 初期化が終わると true になる
window.__lab.errors       // 実行時エラーを蓄積する配列（空が正常）
window.__lab.drop(toyId, platformId, vx, vy, vz, opts)
                           // 指定した台の先端からおもちゃを生成し、
                           // 指定速度で射出する。opts で rotX/rotY/rotZ
                           // （初期姿勢）や spin（初期角速度）も指定可能
window.__lab.state()      // { bodies, lastImpact, fps } を返す
window.__lab.heldScreenPos()
                           // 現在つかまれているおもちゃの画面座標(px)
window.__lab.renderInfo() // { calls, triangles } — そのフレーム全体
                           // （しぶきが2パス描画されるフレームは両パス
                           // 合算）の描画コール数スナップショット
window.__lab.waterDebug() // { pass2Active, dropletsAlive, sprayAlive }
                           // しぶき用2パス目が今フレーム実行されたか、
                           // 水滴/飛沫が生きているか（QA用）
window.__lab.flowState()  // { state, rabbitState, platform } — ゲーム全体の
                           // 状態機械（ready/busy/windup/flight/splashView/
                           // return）とウサギ自身のアニメーション状態、
                           // 現在の飛び込み台IDを返す
window.__lab.throwViaRabbit(vx, vy, vz)
                           // __lab.drop と違い、実際のウサギの振りかぶり→
                           // 離す、というゲーム内経路を通して今持っている
                           // おもちゃを投げる（`ready` 状態で保持中の
                           // おもちゃが無いと何もしない）
window.__lab.megaDrop(giantToyId)
                           // ウサギ経由をすべてバイパスし、そらのだいの
                           // 高さ（SKY.drop）からジャイアントおもちゃを
                           // 直接生成して落とす（'giantheavy' /
                           // 'giantjelly' / 'giantbeach'）。メガ着水の
                           // 7段階の見た目だけを素早く検証したいときに
                           // 使う（気球・ウサギの実際の動きは通らない）
window.__lab.flowState()  // skyMode（そらのだい中かどうか）も返すように
                           // 拡張済み: { state, rabbitState, platform,
                           // skyMode }
window.__lab.balloonScreenPos()
                           // 気球（ゴンドラ）の画面座標(px)。実際の
                           // ポインタタップで気球をタップするテストに使う
window.__lab.cameraDebug() // { mode, pos, lookAt, timeScale, phase,
                           // phaseTimer, slowmoDur, dipAmount, megaElapsed }
                           // — カメラの現在の内部モード・生の位置/注視点
                           // をQA用に公開（本編プレイには無関係）
```

例: `window.__lab.drop('cup', 'high', 0, 0, 0, { rotX: Math.PI })` で、
コップを口が下向きの姿勢のまま高い台からまっすぐ落とせます。

## 検証済み / 未検証

**検証済み（フォトリアルしぶき統合パス — 本パスで実施）**:

- R1（水シェーダー基盤/2パス描画）〜R4（水中ジェット）が並行実装した
  5ファイルを結合し、実際のゲーム内で通しプレイして検証。
  `src/splash.js` / `src/droplets.js` が動的 `import('./watershading.js')`
  で本物の共有シェーダーを使用していること（スタンドアロン用フォール
  バックに落ちていないこと）をランタイムで確認。`waterCtx.uniforms` は
  main が1回だけ生成した共有オブジェクトを splash/droplets/underwater
  すべてが参照しており、`uTimeW`/`uViewport`/`uSunDir` の毎フレーム更新が
  3モジュールへ伝播することを確認。
- **重大な統合バグを発見・修正**: `src/scene.js` の `scene.background` が
  単色 `THREE.Color` であるため、three.js の `WebGLBackground.render()`
  が `renderer.autoClear` の値に関係なく**強制的にフルクリア**する
  （`background.isColor` のとき `forceClear = true` になる仕様）。しぶき用
  2パス目の `renderer.render()` 呼び出しがこの強制クリアを踏んでしまい、
  1パス目で描いたプール・飛び込み台・水面が消え、しぶき（クラウン/
  ジェット/水滴）だけが背景色の上に浮いて見える不具合があった（しぶきが
  発生している間ずっと発生し、収まると自然に直る）。カメラ位置・レイヤー
  マスク・シャドウマップ・アンチエイリアス・グラブパスキャプチャ・
  別カメラインスタンスの使用など他の全変数をA/Bテストで切り分けて原因を
  特定。修正: 2パス目の描画直前だけ `scene.background` を一時的に `null`
  にし（`background===null` は強制クリアされない分岐）、描画後すぐ元の
  `Color` に戻す（`src/main.js`）。あわせて `__lab.renderInfo()` も
  `renderer.info.autoReset` が2回目の `render()` でリセットされるため
  1パス目のみの値しか返せていなかった問題を修正し、両パス合算の値を
  返すようにした。
- 上記修正後、ヘッドレスChromium上でスローモーション連続キャプチャ
  （heavyball/high、8フレーム）で目視確認: 背景（プールタイル・
  飛び込み台）がクラウンの壁・ジェット柱を通して明確に**屈折/歪んで**
  見える、リム（縁）のフレネル反射による明るさ、水滴の太陽グリント、
  チャンキーな水滴とは別レイヤーの白い飛沫（ミスト）、フィンガー
  （指状分裂）が時間とともに成長・分裂する様子、ジェットのくびれ→
  ピンチオフ（先端の水滴分離）まで、すべて目視で確認できた。
- 比較条件が引き続き因果的に区別できることを確認: heavyball vs disc
  （中段、`energy`/`flatness` が明確に異なる）、まっすぐ vs 斜め投げ
  （heavyball中段、vx=4 で `oblique` が 0→0.36 に変化）、低 vs 高
  （heavyball、`speed`/`energy` が単調に増加）。
- 決定性: heavyball/mid を2回リロードして再ドロップ、`seed` が完全一致
  （同じマクロ形状になることを確認）。
- フルスモークテスト: 8種トイすべてを中段台から順にドロップ（リロード
  はさみつつ）、全ケースでインパクト発火・エラーなし。コップを
  `rotX: Math.PI`（口が下向き）で高台から落とすと `cupTrap≈1` の泡が
  発生、ビーチボールは着水後もエラーなく浮上/リサーフェス。
- 入力回帰テスト: タップドロップ・フリック投げ・エイムアシスト・
  ミスグラブ耐性・トイ/台ボタン切り替え・リサイズ・連打の9項目すべて
  PASS（`scratchpad/input-verify.mjs`）。
- 描画パフォーマンス（修正後、両パス合算の正しい値）: アイドル時の
  描画コール数 約22回、3つのしぶきが重なるバーストのピークでも
  約38〜41回（予算<80コールを十分に下回る）。しぶきが何も無いときは
  `__lab.waterDebug().pass2Active` が `false` になり2パス目が実際に
  スキップされることを確認。fps はヘッドレスSwiftShaderで約15〜20
  （後述の通り実機性能の指標にはならない）。
- `window.__lab.errors` / コンソールエラー / ページエラー: 上記すべての
  シナリオを通じて0件。
- 全変更ファイル（`src/main.js` / `src/watershading.js` / `src/splash.js`
  / `src/droplets.js` / `src/underwater.js`）で `node --check` 構文チェック
  OK。デバッグ用に一時追加したグローバル・console.log は最終コミット前に
  すべて除去済み（`window.__lab` のみ）。

**検証済み（ウサギ体験オーバーホール統合パス — S5 実施）**:

- S1（`src/rabbit.js`）〜S4（`src/input.js`）が並行実装した4ファイルを
  結合し、ヘッドレスChromium（SwiftShader）でポートレート/ランドスケープ
  両方向、実ポインタ操作（タップ・ドラッグ）で通しプレイして検証。
- **重大な統合バグを2件発見・修正**:
  1. `src/cameraFX.js` の `setIdleView()` はコンストラクタと
     `returnToTower()` の内部からしか呼ばれておらず、**投げを伴わない
     台タップ移動（例: 中→高へ台だけタップして移る）ではアイドル
     カメラが新しい台へ再フレーミングされず、古い台を映したまま固定
     されてしまう**不具合があった。修正: `src/gameflow.js` の
     `_climb()` 完了時（実ウサギ経路・フォールバック経路の両方）に
     `cameraFX.setIdleView(新しい台のtip)` を呼ぶよう追加。
  2. `src/input.js` のタップ判定は「台に当たったら台移動」「ドラッグ
     したら投げ」の2分岐しかなく、**台に当たらない場所への素タップ
     （契約が要求する『plain tap = ほぼ垂直な落下』）が何も起きない**
     不具合があった（ドラッグ量がしきい値未満だとそもそも `beginAim`
     が呼ばれないため）。修正: 台に当たらなかった素タップ時に
     `velocity=(0,0,0)` で `gameflow.commitThrow()` を直接呼ぶ分岐を
     追加。
  3. **見た目の統合問題として発見・修正**: アイドル時のカメラ
     （3/4俯瞰、方位25°）だと、ウサギが両手で掲げているおもいボール
     （半径0.35m）が、ウサギの本体とほぼ同じ視線（カメラからの
     奥行き方向）に来てしまい、**起動直後のアイドル画面でウサギの
     姿がボールにほぼ完全に隠れて見えなくなる**（起動時の受け入れ
     条件「ウサギがボールを掲げて台の上に立っている」を満たせない）
     ことをスクリーンショット目視で発見。カメラ距離・高さを変えても
     改善しないことを実測で確認（ウサギとボールの奥行きオフセットは
     ほぼ純粋に世界X軸方向で、方位角を変えない限り視線に対して常に
     ほぼ正面に重なるため）。修正: アイドル表示専用の方位角
     `IDLE_AZIMUTH_DEG`（68°、`src/cameraFX.js`）を追加し、
     followFlight/splashView が使う方位（25°）とは独立に、アイドル
     時だけウサギの真横寄りから見るようにした。台の見やすさは維持した
     まま、ウサギの姿（耳・体・持っているおもちゃ）がはっきり見える
     ようになったことをスクリーンショットで確認。
- **ユーザーからの元の不満2点の解消を実測で確認**:
  1. 「高い台から落とすと、しぶきを楽しむ前にカメラが台へ戻って
     しまう」→ 高台から `throwViaRabbit(0,0,0)` で直落下させ、状態が
     `ready→windup→flight→splashView→return→ready` の順に正しく遷移
     すること、`splashView` の継続時間が実測 約3.46秒（契約の下限
     2.0秒を満たす）、`splashView` 中のカメラ視線と着水点のなす角が
     13〜16°（許容25°未満）で、着水点をロックし続け一度もタワー側へ
     逸れないことを確認。スクリーンショット連番でクラウン→ジェットの
     ペイオフ（発光する屈折ジェット柱）まで明確に見えることを確認。
  2. 「常時表示のボタンが着水地点に重なる」→ `flight`/`splashView`
     中は `.h-toybar` の `opacity` が実測 `0`・`pointer-events:none`
     になり、`return`/`ready` で `opacity` が `1` 近くまで戻ることを
     ポートレート・ランドスケープ両方で確認（左側の台ボタン列は
     この改修で完全撤去されたため、そもそも重なりようがない）。
- ウサギのワークフロー（台タップで移動→はしごを登り/降りる→箱から
  次のおもちゃを取り出して掲げる、トイボタンで持ちものを箱にしまって
  別のものに交換）を実ポインタ操作で確認。斜め投げ
  （`throwViaRabbit(2,-3,1)`）で `oblique>0` のインパクトになること、
  ドラッグでの狙い（点線の弧プレビュー＋ウサギの傾き）と、乱暴な横
  ドラッグでもエイムアシストにより着水点がプール半径5.2m以内に収まる
  ことを確認。素タップ（ドラッグなし）では `oblique=0` の垂直落下に
  なることを確認（上記バグ修正後）。
- 回帰確認: `__lab.drop()` はウサギ経由の投げを介さず直接
  `ready`/`busy` から `splashView` へ強制遷移し、その後 `return`→
  `ready` まで正しく回復することを確認（ウサギなしでも動作）。
  因果関係の数値（heavyball低/高エネルギー 0.872/1.000、disc
  flatness 1.0）、決定性（heavyball/mid のシード `2998442878` が
  複数回のロードで完全一致）は本パスでも変化なし。8種トイの
  スモークテスト・フォトリアルしぶきの見た目（屈折・クラウン・ジェット）
  も健在。
- 5連続投げ（速度を変えて）で毎回 `ready` に復帰、エラーなしを確認。
  飛行中・しぶき中に台タップ／トイボタンを連打する耐性テストでも、
  リクエストがキューされて後で処理される・状態機械がスタックしない
  ことを確認（エラーなし）。
- 描画コール数: アイドル時 約32回、しぶきバースト時 約27〜36回
  （予算<90を十分に下回る）。起動から `ready` までの時間は約2.3〜
  2.6秒（ヘッドレスSwiftShaderでの計測、契約の目安6秒を十分に下回る）。
- `window.__lab.errors` / コンソールエラー / ページエラー: 上記すべての
  シナリオを通じて0件。
- 全変更ファイル（`src/rabbit.js` / `src/cameraFX.js` / `src/gameflow.js`
  / `src/main.js` / `src/input.js`）で `node --check` 構文チェックOK。
  デバッグ用に一時追加したグローバル（`window.__debugRabbit` 等）は
  検証完了後にすべて除去済み（`window.__lab` のみ残存）。

**検証済み（メガ着水「そらのだい」統合パス — M6実施）**:

- M1（気球/ウサギのゴンドラ搭乗）〜M5（フロー/カメラ/音/入力/main配線）
  が並行実装したファイルを結合し、ヘッドレスChromium（SwiftShader）で
  ポートレート/ランドスケープ両方向、実ポインタ操作（気球タップ→上昇→
  ドラッグで狙う→はなして投げる、まで）で通しプレイして検証。
- **統合バグを6件発見・修正**:
  1. **気球が起動直後の画面に一切映っていなかった**（`src/scene.js` の
     係留位置 `mooredPos`）。M1契約の「起動時から見える」という要件に
     反していた（ポートレート・ランドスケープどちらも画面外）。修正:
     係留位置のオフセットを調整し、両方向でストライプ模様の気球本体＋
     ゴンドラが画面に収まるようにした。
  2. **持ち上げたジャイアントおもちゃが気球の袋（エンベロープ）に
     めり込んで見える**（供給された仕様レビュー担当からの懸念(a)）。
     ゴンドラの持ち手位置（床+2.2m）に対し、気球エンベロープの根元
     （首）が低すぎ・小さすぎたため、どのジャイアントおもちゃ（最大
     半径1.5m）を持たせても布地に埋まって見えていた。修正:
     `src/scene.js` のエンベロープの首の高さを引き上げ（最大サイズの
     おもちゃの上端より確実に高くなるよう）、半径も1.2倍に拡大して
     バスケットに対して見劣りしないサイズ感にした。
  3. **上昇完了後、カメラがウサギ・ゴンドラ・持っているおもちゃを
     まったく映さなくなる**（懸念(b)）。`ascendView` は上昇中の演出
     専用で、上昇が終わったあとにカメラを別のビューへ切り替える処理が
     どこにも無く、上昇時の「世界がどんどん小さくなる」広角のまま
     固定されてしまっていた（ウサギもおもちゃも画面外）。修正:
     `src/cameraFX.js` に専用の `skyReadyView` モードを新設し、
     `src/gameflow.js` が「そらのだい」の準備完了時（初回上昇後・
     メガ着水からの復帰後）に呼び出すようにした。
  4. 上記(3)を直しても、**ウサギがおもちゃの真後ろに完全に隠れて
     見えなかった**。持っているおもちゃの位置（ゴンドラ中心の真上）と
     ウサギ自身の立ち位置がまったく同じ水平座標で、どの方位から見ても
     ウサギがおもちゃの陰に入ってしまっていた。修正:
     `src/rabbit.js` にゴンドラ内での立ち位置オフセット
     （`GONDOLA_STAND_OFFSET_X/Z`）を追加し、地上の飛び込み台と同じ
     「持ち手位置と立ち位置を離す」設計をゴンドラにも適用。
     `skyReadyView` 側もバスケットのふちより高い位置から見下ろす角度に
     調整し、ジャイアントおもりボール／ジャイアントゼリーボールでは
     ウサギの姿が確認できるように改善（ジャイアントビーチボールは
     直径がバスケット幅とほぼ同じため、この改善後もほぼ隠れてしまう
     ケースが残る — 既知の限界として記録）。
  5. **落下中のフォローカメラ（`followFlight`）の終端位置が、メガ着水
     カメラ（`megaSplashView`）の基準距離と大きくズレていた**
     （後者の半分程度の距離）ため、着水直後の1秒近く、いちばん派手な
     はずの「①フラッシュ＋衝撃ディスク」「②白いドーム」が古い遠景の
     まま映ってしまっていた。修正: `src/cameraFX.js` の
     `FOLLOW_MEGA_DIST_END`/`FOLLOW_MEGA_Y_END` を `megaSplashView` の
     基準値に揃え、既存の通常投げと同じ「継ぎ目のない引き継ぎ」原則を
     メガ版にも適用。
  6. **ゴンドラを見下ろすリアクションカット（E3カット）が、実際には
     何もない空を映していた**。カメラは実際のシーン参照を持たない設計
     のため、ゴンドラのだいたいの位置（`GONDOLA_APPROX`）という固定値
     を使っていたが、これが実際のゴンドラ位置から数m以上ズレており、
     カットの間ずっと空だけが映っていた。修正: `ascendView`/
     `skyReadyView` が読んでいる実際のゴンドラ位置を毎フレームキャッシュ
     しておき、E3カットと`returnFromMega`の着地目標の両方がこのキャッシュ
     値を使うようにした。
- `__lab.megaDrop()` で3種のジャイアントおもちゃそれぞれの7段階を
  スクリーンショット連番（状態変化に応じたタイミングで撮影）で確認:
  ①フラッシュ＋衝撃ディスク、②白く沸き立つドーム、③壁のような大波
  （ドームの縁の広がりとして視認）、④ふつうのジェットよりはるかに太い
  水柱、⑤きのこ雲状の水しぶきの雨、⑥スロッシングによる水面の傾き、
  ⑦泡の白さが引いていく様子、すべて確認。ジャイアントゼリーボールでは
  着水点周辺に複数のピンク色のかけらが飛散し、それぞれが独立した
  二次しぶきを起こす「ゼリーの雨」を確認。ジャイアントビーチボールでは
  ドームの中に一度沈んだビーチボールが見え、その後浮上して水面に
  浮かぶ様子、水面の傾き（2つめの波の兆候）を確認。
- 実際のウサギ経路（気球タップ→busy→上昇→ready+skyMode→ドラッグで
  狙う→はなして投げる→長い滞空→メガ着水→復帰→次のジャイアント
  おもちゃ）を実ポインタ操作で最初から最後まで確認。復帰後もそらのだい
  に留まり、中段の台をタップすると気球が降りて通常フローに正しく戻る
  ことも確認。
- **回帰ゲート（すべてPASS）**: heavyball低/中/高段のエネルギー
  0.8719756548962352 / 1.0 / 1.0（契約の 0.872/1.000/1.000 と一致）、
  heavyball中段のシード `2998442878`（契約と完全一致）、disc平置き
  flatness=1.0、cup（`rotX: Math.PI`）cupTrap≈1.0000000000000002。
  通常しぶきの見た目（クラウン/ジェット/水滴の屈折）に変化なし。
  素タップ（プール上の任意の場所を軽くタップ）→ `oblique=0` の垂直
  落下になることを確認（入力回帰）。3連続 `megaDrop`（リロードなし）で
  毎回 `splashView`→`return`→`ready` まで正しく回復、スタックなしを確認。
- 描画コール数: アイドル時 41回、通常しぶきバースト時 43回、メガ着水
  ピーク時 最大50〜77回（ジャイアントゼリーボールの複数二次しぶき時が
  最大。予算<110コールを十分に下回る）。起動から `ready` までの時間は
  約1.6秒（Playwright起動オーバーヘッド込み、ヘッドレスSwiftShaderでの
  計測）。
- `window.__lab.errors` / コンソールエラー / ページエラー: 上記すべての
  シナリオを通じて0件。
- 全変更ファイル（`src/scene.js` / `src/rabbit.js` / `src/cameraFX.js` /
  `src/gameflow.js` / `src/main.js`）で `node --check` 構文チェックOK。
  デバッグ用に追加したのは `window.__lab.balloonScreenPos()` /
  `window.__lab.cameraDebug()` のみで、いずれも既存の
  `heldScreenPos()`/`waterDebug()` と同じ趣旨のQA用read-onlyヘルパー
  として恒久的に残置（`window.__lab` 以外のグローバルなし）。

**未検証（本パスの対象外）**:

- 実機の iPhone / iPad（今回はすべてヘッドレス Chromium + SwiftShader
  ソフトウェアレンダリングでの検証）
- 実機でのタッチ操作の感触・ハプティクス（振動フィードバック）
- 実機でのフレームレート・GPU性能。ヘッドレスSwiftShaderの fps
  （アイドル時 約18fps、しぶきが重なる場面で約15fps）は**ソフトウェア
  レンダリングの数値であり実機のGPU性能を代表しない**。実機
  （特にiPhone/iPadのGPU）では大幅に高いfpsが期待できる。描画コール数・
  三角形数は実機性能に直結する指標として計測済み（上記参照）だが、
  実際のfpsは実機での確認が必要。
- **WebGL1 フォールバック経路（`NO_GRAB` 変種）**: `waterGlslCommon(false)`
  が生成する GLSL（`refractSample` が背景サンプルの代わりに単色の
  「深い水」フォールバックを返す変種）は、ブラウザ内で直接呼び出して
  文字列として正しく生成されること、`#define HAS_GRAB` が含まれないこと
  は確認した。また `src/main.js` が `renderer.capabilities.isWebGL2` が
  `false` の場合に `grabPass` を作らず `uSceneTex` が `null` のまま
  `sharedWaterUniforms(null)` を配ること、`splash.js`/`droplets.js`/
  `underwater.js` それぞれが `grabPass`/`uSceneTex` の欠如を検知して
  `hasGrab=false` の材質を選ぶことはコードレビューで確認した。ただし
  **実際にWebGL1コンテキスト（またはWebGL2でグラブパス無効）で描画させて
  見た目を確認する実機/実ブラウザテストは行っていない**
  （このヘッドレス環境の Chromium + SwiftShader は常にWebGL2を報告する
  ため、強制的にWebGL1相当にする手段がなく未検証）。

## 見つかった問題

以下は、フォトリアルしぶき統合パスより前の受け入れパスで見つかった軽微な
既知事項です（このセクション自体は当時のまま）。フォトリアルしぶき統合
パスで見つかった重大な統合バグ（`scene.background` 強制クリア問題）と、
ウサギ体験オーバーホール統合パスで見つかった統合バグ（アイドルカメラの
再フレーミング漏れ・素タップが無反応・ウサギがボールに隠れる見た目問題）
とその修正は、上の「検証済み / 未検証」内にそれぞれ記載しています。

- **同時オブジェクト数の一時的なオーバーシュート**: `src/physics.js` の
  `MAX_BODIES`（6）を超えると古いオブジェクトをフェードアウトさせて
  数を絞る仕組みがあるが、フェード対象は「静止中（floating/sunk）」の
  オブジェクトに限られる。12回連続投入のような高速連投では、まだ飛行中/
  着水直後のオブジェクトが多く、フェード候補が足りずに一時的に上限を
  超える（今回の計測では最大9個）。数秒後には6個まで戻り、無限に増え
  続けることはない。プレイ中に4歳児が連打しても致命的な破綻にはならない
  想定だが、余裕を持たせたい場合は `MAX_BODIES` を7〜8に上げる、または
  フェード対象の判定条件を緩める（例: 着水後一定時間が経過していれば
  flying中でもフェード対象にする）などの微調整が考えられる。
