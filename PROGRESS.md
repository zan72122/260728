# りんりん！ちいさなのりもの修理屋さん — 実装記録

4歳児が、文字を読まずに、指1本で遊べる「のりもの修理」ゲーム。
こぐまの三輪車の不具合を直す **5つのエピソード** が入っている。

| # | エピソード | 症状 | 直しかた（すべて指1本） |
| --- | --- | --- | --- |
| ① | パンク | タイヤがぺしゃんこ・穴から空気漏れ | 押してへこみ発見 → パッチ3択を貼る → ポンプ3回 → 弾いて回す |
| ② | ベルが鳴らない | 押しても「…」の煙 | 2回ためすとフタが開く → 部品3択をはめる → ネジをくるくる3回 → 押して「りんりん！」 |
| ③ | ぐらぐらハンドル | ハンドルが揺れて蛇行 | さわると揺れてネジが光る → スパナを運ぶ → 円をなぞって3回締める |
| ④ | キーキーきしむ | 車軸からギザギザの稲妻 | 油さしを運ぶ → 押してポタッ×3 → 弾いて回して確認 |
| ⑤ | どろんこ | 車体にどろ8か所 | スポンジでこすって落とす → 布でみがいてピカーン×3 |

タイトル画面に**絵だけのエピソード選択**（5つの丸アイコン）があり、
直し終えたものには金の星がつく。大きな再生ボタンは「まだ直していない次のもの」を始める。

---

## 1. 何を作ったか

| 項目 | 内容 |
| --- | --- |
| 形式 | ビルド不要の静的サイト（HTML + CSS + ES Modules） |
| 描画 | Canvas 2D にすべて手描き（画像アセット・外部ライブラリ ゼロ） |
| 入口 | `index.html` |
| 動かし方 | リポジトリ直下で `npx http-server -p 8123` → `http://localhost:8123/` |

### ファイル構成

```
index.html          画面の器（フルスクリーン canvas / セーフエリア計測用の probe）
css/style.css       スクロール・ピンチズーム・長押しメニューを止める土台
js/main.js          起動とメインループ（1フレームこけても止まらない）
js/game.js          状態遷移・カメラ・あそびのルール
js/art.js           絵の部品すべて（こぐま・三輪車・タイヤ・道具・UI）
js/particles.js     空気のもや / キラキラ / 紙ふぶき / ハート
js/input.js         一本指だけを通すポインタ入力
js/audio.js         WebAudio による効果音の合成（無音でも成立する飾り）
js/util.js          補間・イージング・2関節IK などの小道具
tools/playtest.mjs  Playwright で全工程を自動プレイする検証スクリプト
```

---

## 2. あそびの流れ

どのエピソードも同じ骨組みで進む。

| 場面 | 内容 | 進む条件 |
| --- | --- | --- |
| タイトル | 再生ボタン＋エピソード5択（絵のみ） | どこかを触る（アイコンならそれを選ぶ） |
| 登場 | こぐまが**症状を見せながら**走ってきて止まる（ガタガタ／「…」の煙／ハンドル暴れ／キーキー／どろんこ） | 自動（約5秒） |
| 発見 | 症状のある場所を触ると、症状が強調されて原因が光る | 1〜2回触る |
| 修理 | エピソードごとの道具とジェスチャ（貼る・はめる・回す・運ぶ・押す・こする） | 各3回 or 完了まで。回数は丸3つの点灯で示す |
| 確認 | 直った実感の一手（車輪を弾く／ベルを鳴らす／ピカーン） | 1回で成立 |
| 出発 | こぐまが乗り、大きな再生ボタン | 触る（6.5秒放っておくと自動発進） |
| 試運転 | **17秒**の遠乗り。上のリボンが進み具合を示す。**直した内容が走りに出る**（後述） | ゴールのアーチに到着 |
| ゴール | 紙ふぶき・メダル・出むかえの友だち | 円形矢印ボタンでタイトルへ（金の星がたまる） |

試運転への反映：パンク＝ガタガタ→なめらか、ベル＝タップでいつでも「りんりん！」（音符と小鳥が飛ぶ）、
ハンドル＝ハンドルの揺れが消えてまっすぐ、油＝ギザギザが消えハブがきらめく、洗車＝車体が輝きをまとって走る。

---

## 3. 受け入れ条件にどう答えたか（設計メモ）

### 文字を読まずに遊べる
- 指示は **「光るリング」＋「お手本の手」** の2つだけで統一。1.6秒さわらないと、
  その場面のジェスチャーを手のゴーストが繰り返し実演する（タップ／運ぶ／押し下げる／弾く）。
- 数字を使わない：ポンプの回数は**丸3つの点灯**、試運転の進み具合は**リボンと旗**、空気の量は**圧力計の針**。
- タイトルの日本語は装飾。読まなくても、脈打つ再生ボタンだけで始められる。

### 一本指だけ
- `input.js` が最初の 1 ポインタだけを追跡し、2本目以降の `pointerdown` は捨てる。
  手のひらがついてもゲーム状態は変わらない。
- 使う操作は「触る／なぞる」だけ。ピンチ・長押し・ダブルタップは一切要求しない。

### iPhone / iPad の縦横
- レイアウトは**アスペクト比から毎フレーム再計算**する。固定デザイン幅は持たない。
- 道具置き場（ドック）は **縦画面＝下 / 横画面＝右**。カメラが使う矩形はドックを避ける。
- カメラは「地面の線を画面の下 82% に置く」方式なので、縦長でも横長でも
  空・のりもの・草の比率が崩れない。
- `viewport-fit=cover` ＋ `env(safe-area-inset-*)` を JS から実測し、
  ノッチ／ホームインジケータにボタンが潜らないようにしている。

### 精密操作を要求しない
- タイヤの当たり判定は半径 1.5 倍。
- パッチの吸着範囲は「画面短辺の 30%」か「車輪半径の 1.5 倍」の**大きいほう**。
  ドラッグ中は吸着範囲が破線の円で見える。外しても**戻るだけ**で減点はない。
- ポンプはポンプ周辺の広い矩形をつかめる。取っ手は指の縦移動に相対追従。
- 車輪は「少しでも回れば」成立。ゆっくり回しても勢いを足してくれる。

### 罰がない
- タイマー・スコア・残機・ゲームオーバーは存在しない。失敗した操作は「何も起きない」。
- パッチは3種類とも正解。選んだ形と色はそのままタイヤに残り、試運転でも回り続ける。

### 音なしで理解できる
- 進行判定は視覚情報のみ。音は WebAudio の合成音で、右上のボタンでいつでも消せる。
- AudioContext が使えない環境では、音まわりは黙って無効になる。

### 乗り手が「こぐま」である理由（脚の解剖）

ペダルをこぐ姿を破綻なく描けるかどうかは、**その動物が蹠行（せきこう）性かどうか**で決まる。

- **蹠行性**（かかとを地面につけて歩く）＝ クマ・サル・ネズミ・アライグマなど。
  太もも → **前へ曲がる膝** → **平たい足の裏** をそのまま描いてよい。
- **指行性 / 蹄行性** ＝ ウサギ・ネコ・イヌ・ウマ・シカ・鳥。
  脚の途中で後ろに折れているのは膝ではなく**踵**。人間の膝のように描くと違和感が出る。
- さらにウサギ・カエル・カンガルーは跳躍動物で、**後肢を左右交互に動かせない**。
  交互ペダリングは生物として成立しない。

初版はウサギだったが、上記2点により「膝のない棒」に見えていた。
蹠行性で、絵本に二足歩行の前例が最も多い **こぐま** に差し替えた。

実装上は次の3つを直している。

1. **膝の向き** — IK の曲げ方向を反転（`bendSign = -1`）。自転車をこぐ人と同じく膝が前・上へ折れる。
2. **手足の形** — 等太さのカプセル2本をやめ、**2円の外接接線で作る「先細りの塊」**に。
   付け根が太く先が細いので、太ももの量感が出る。
3. **足** — 丸ではなく**かかとが後ろに出た足の裏**を描き、肉球側をクリーム色に。
   ペダルには足の裏が乗り、こぐリズムに合わせて足首がわずかに返る。

三輪車の寸法（サドルの高さ・前輪の大きさ・クランク長）は、
**「腰からペダル最遠点までの距離 ≒ 脚の長さ」**になるよう乗り手から逆算した。
そのため一周のどの位置でも膝が自然に曲がり、伸び切って棒になる瞬間がない。

### 修理の出来が試運転に出る
- タイヤの半径を角度の関数 `wheelRadiusAt()` として持ち、
  **接地面のつぶれ（地面に対して固定）＋ゴムのいびつさ（回転に追従）＋へこみ＋パッチの厚み**
  を合成している。
- 車軸の高さはこの関数の「真下の値」そのもの。だから
  空気が少ない＝車軸が上下に暴れる＝ガタガタ走行、
  空気が満タン＝ほぼ真円＝なめらか走行、が**自動的に**そうなる。
- 登場シーン（ぺしゃんこ）と試運転（修理後）で同じ計算を使うので、
  子どもは「直したから静かに走っている」を見比べられる。

---

## 4. 検証（`tools/playtest.mjs`）

Playwright + Chromium で、実際に指の動きを再現して全工程を通す。

- 端末プリセット：`iphone` / `iphone-land` / `ipad` / `ipad-land` / `iphone-se`
- 各段階でスクリーンショットを保存し、状態・空気圧・パッチ・へこみの数を記録する
- `console` のエラー、`pageerror`、リクエスト失敗をすべて収集し、
  1件でもあれば終了コード 1 で落とす

```
node tools/playtest.mjs --device=iphone --shots=/tmp/shots   # ①パンクの通しプレイ
node tools/episodes-test.mjs --device=iphone --shots=/tmp/shots  # ②〜⑤の通しプレイ
node tools/robustcheck.mjs                                   # 壊れにくさ
```

`tools/episodes-test.mjs` は4エピソードを連続で実プレイする：
ベル（タップ→部品をずらして落として吸着→円ドラッグでネジ3回転→鳴らす）、
ハンドル（タップ→スパナを運ぶ→3回転で wobble=0 を確認）、
油（油さしを運ぶ→3滴で squeak=0 を確認→弾く）、
洗車（ストロークで泥8か所を全消し→みがき3回）。
最後にタイトルの金の星が4つ揃うことも確認する。

`tools/robustcheck.mjs` は遊びの流れ以外を見る：
本物のタッチイベントでの開始、**2本目の指の無視**、音の ON/OFF、
**遊んでいる途中の画面回転（縦↔横、iPhone↔iPad サイズ）**、放置時のお手本表示、長時間の試運転。

### 結果

| 端末プリセット | 最後まで到達 | 試運転の長さ | コンソールエラー |
| --- | --- | --- | --- |
| iPhone 縦 390×844 | ○ | 17.3 秒 | 0 |
| iPhone 横 844×390 | ○ | 17.4 秒 | 0 |
| iPad 縦 820×1180 | ○ | 17.3 秒 | 0 |
| iPad 横 1180×820 | ○ | 17.2 秒 | 0 |
| iPhone SE 375×667 | ○ | 17.0 秒 | 0 |

走りのガタつき（車軸が 0.55 秒間に上下した割合）:

| | パンクしたまま（登場シーン） | 修理後（試運転） |
| --- | --- | --- |
| 計測値 | 0.152 | 0.008 |

同じ計算式で **約 19 倍** の差。パッチを貼って 3 回空気を入れた結果が、
そのまま走りのなめらかさとして出ている。

`robustcheck.mjs` は 14 項目すべて OK、コンソールエラー 0 件。

---

## 5. 作業ログ

1. 空のリポジトリから、Canvas 2D の描画基盤・単指入力・パーティクル・合成音を実装。
2. 乗り手 / 三輪車 / タイヤの変形モデルを作り、状態遷移をつないで一通り遊べる形に。
3. Playwright で自動プレイし、スクリーンショットを1枚ずつ確認して修正：
   - 縦長画面で草原ばかりが写る → カメラを「地面基準」の構図固定に変更
   - 道具トレイが小さすぎる → 画面下（横画面は右）にせり出す木のドックへ作り直し
   - タイヤのへこみがリムを突き抜ける → 変形量に下限を設定
   - 登場シーンのあと穴が裏側に隠れることがある → 穴が右斜め上に来るまで車輪を回して停止
   - ゆっくり弾くと車輪の回転が認識されない → 回した角度の合計でも判定するよう緩和
   - favicon の 404 → SVG データ URI を埋め込み
   - 乗り手の手足が体と同じ色で消える → 手足と輪郭に薄い線を追加
   - ホースがポンプの上を横切る → ベジェの制御点を作り直して本体の左へ垂らす
   - 走りのガタつきの指標が鈍い → 1フレーム差分から「0.55秒の上下幅」に変更（差が19倍に）
   - 横画面で音ボタンとパッチ・回数表示が重なる → 音ボタンを常に左上へ、回数表示を移動
   - 大きな丸ボタンが乗り物に重なる → 縦は下中央 / 横は右下に置き分け
   - タブレットで車輪が大きくなりすぎる → 寄りの画角を画面サイズに追従させる
4. 縦横・端末サイズを変えて再検証し、コンソールエラーが出ないことを確認。
5. 描画が重い環境向けに、コマ落ちが続いたら描画解像度だけを静かに下げる仕組みを追加
   （検証環境の GPU なし Chromium で 15fps → 60fps）。
6. **乗り手をウサギからこぐまへ差し替え**（受け入れ条件を「動物の三輪車」に変更）。
   ウサギの後ろ足は指行性のZ字で、人間の膝のようには描けないため、
   蹠行性のこぐまに変更したうえで、膝の向き・手足の塊・足の裏を作り直した。
   あわせて三輪車の寸法を乗り手の脚の長さから逆算し直し、
   ペダル一周のどの角度でも膝が自然に曲がることを4姿勢の拡大画像で確認。
   ゴールの友だちも毛色ちがいのこぐま2匹に。
7. **エピソードを5本に拡張**（ベル・ハンドル・油・洗車を追加）。
   - 状態機械を「発見→道具→ジェスチャ→確認→試運転」の骨組みに一般化。
     ドックからの「大まかドラッグ＋吸着」、回数の丸表示、間を置いた場面送り（after()）を共通化した
   - 新ジェスチャ3種：**円をなぞって回す**（角度を積算、1回転弱で1カウント。ベルのネジとスパナで共用）、
     **押してしずくを落とす**（落下と着弾のはねを物理で）、**こすって消す**（泥ごとに体力、スポンジの移動量で減る）
   - ベルはフタが開く・部品が入る・ネジが回る・鳴って波紋、を1つの描画関数で状態表示
   - カメラの寄り先をエピソードで切替（タイヤ＝前輪、ベル/ハンドル＝ハンドルまわり、洗車＝車体全体）
   - タイトルに絵だけのエピソード選択と、直し終えた印（金の星）を追加
   - 油さしの構え位置がかごに重なっていた・回数の丸がかごに重なっていた、を screenshot 確認で修正
   - 検証：episodes-test.mjs が iPhone 縦 / iPad 横で 4 エピソードとも finale まで到達、
     失敗 0・コンソールエラー 0。既存のパンク通しプレイと壊れにくさ14項目も引き続き全部 OK。
